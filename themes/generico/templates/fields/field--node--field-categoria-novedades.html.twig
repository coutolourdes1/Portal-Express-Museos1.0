{% if label_hidden %}
    {% for item in items %}
    <div class="text__header">
      <div{{ attributes.addClass(classes, 'label__primary') }}>{{ item.content }}</div>
    </div>
    {% endfor %}
{% endif %}
